#!/bin/bash

initialize-anyenv-environment() {
    # Removed because I use asdf now.
    # if type anyenv >/dev/null 2>&1; then
    #     export PATH="$HOME/.anyenv/bin:$PATH"
    #     eval "$(anyenv init -)"
    # fi
}
